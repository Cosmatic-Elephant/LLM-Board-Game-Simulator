# 작업 진행 기록

세션 1~10의 상세 기록은 `HISTORY.md` 참고. 이 파일은 세션 11부터 간결하게 기록한다.

---

## 세션 10 (완료) — 멀티플레이 핵심 루프 구현

- Socket.io 방 생성/참가/퇴장, 색상·AI 모델·게임 설정 실시간 동기화 (`server.ts`, `src/app/page.tsx`)
- 멀티플레이 게임 시작 → `/multi` 페이지 → 서버 권위(server-authoritative) `GameState` 브로드캐스트
- 서버가 싱글플레이 게임 엔진(`game-engine.ts`, `bill-setup.ts`)을 그대로 재사용해 굴리기·베팅·정산·다음 라운드 전체 사이클 동작
- LLM/깡통은 전부 무작위 자동 진행(실제 LLM API 미연동), `/multi` UI는 정적 렌더링만(연출 없음), 이탈/타임아웃 처리 없음

---

## 세션 11 (완료, 2026-07-10~11) — 멀티플레이 완성도 작업

- **이탈/종료 처리**: 게임 중 호스트 이탈 시 방 삭제 + `host-left`, 게스트 이탈 시 해당 슬롯 깡통 대체 + `player-left`, 호스트의 명시적 "게임 종료"는 `end-game`/`game-ended`로 전원 퇴장. 우측 하단 나가기 버튼(호스트/게스트 문구 분기, 기존 `leave-room`/`end-game` 재사용)
- **턴 타임아웃**: 사람 플레이어 15초 미행동 시 서버가 깡통처럼 자동 행동, 5초 이하 카운트다운 UI + 굴리기 버튼/카지노 테두리 점멸(본인 클라이언트만)
- **실제 LLM API 연동**: 서버의 자동 베팅 로직을 `getLLMAction()` 호출로 교체(단일 액션 최적화·깡통 랜덤 폴백 유지, 실패 시 랜덤 대체)
- **베팅 말풍선**: 사람/봇/타임아웃 구분 + LLM reasoning 포함, 서버가 `bet-bubble`로 구조화된 정보 전송
- **턴 연출 이식**(싱글 → 멀티): 주사위 그룹화, 프리롤 페이드인, 굴리기 셔플, 베팅 퇴장 애니메이션 — 서버 브로드캐스트를 지연 반영하는 상태 큐(`stateQueueRef`/`processQueue`) 아키텍처로 구현
- **호버 하이라이팅**: 카지노↔주사위 상호 하이라이팅(클라이언트 전용), 타임아웃 점멸보다 호버가 우선 표시
- **정산 연출 이식**(싱글 → 멀티): 서버가 정산 전 카지노 스냅샷 + 단계별 이벤트(`scoring-steps`)를 먼저 보내고 round-end/game-end `game-state`가 뒤따름, 클라이언트가 싱글과 동일한 순서로 재생(동률 배제 → 순위 하이라이트 → 지폐 페이드+소지금 증가 → 테이블 클리어), 스킵 버튼(호스트 전용, `skip-scoring`/`scoring-skipped`)
- **버그 수정**: 멀티 종료 후 싱글 로비 설정이 남은 sessionStorage 값에 가려지던 문제(죽은 코드 제거), 베팅 말풍선이 연출 시작 전에 너무 일찍 뜨던 문제(큐로 지연)

---

## 현재 상태

멀티플레이가 방 생성부터 게임 종료까지 싱글플레이와 대등한 연출 수준으로 엔드투엔드 동작한다. 굴리기·베팅·이탈·타임아웃·정산 전 과정에 애니메이션과 말풍선이 붙어 있고, LLM 플레이어는 실제 API를 호출한다. `src/app/game/page.tsx`(싱글플레이)는 이번 세션 내내 건드리지 않았다. `npx tsc --noEmit` 에러 없음, `npm run dev` 부팅 정상 확인.

---

## 미결 사항 (누적)

- [ ] q키 테스트 단축키 — 최종 배포 전 삭제 필요(싱글, `// TEST ONLY` 주석 위치)
- [ ] 에러 핸들링: LLM API 키 미설정/호출 실패 시 사용자 안내 UI 없음(서버는 실패 시 랜덤 폴백하지만 알림 없음)
- [ ] `npm audit` 경고 — Next.js 내부 postcss 취약점, Next.js 업스트림 패치 대기로 보류
- [ ] NVIDIA NIM 모델 미지원
- [ ] 정산 연출 중 말풍선이 오래 남아 있음 — 다음 세션에서 처리 예정
- [ ] 싱글플레이 라운드 종료 화면에 멀티 같은 중앙 정렬 연출이 없음 — 다음 세션에서 이식 예정
- [ ] 라운드 마지막 베팅은 퇴장 애니메이션 없이 곧바로 정산 연출로 넘어감(의도된 단순화 — 정산으로 카지노가 초기화되기 전 상태를 diff할 수 없어 생략)
- [ ] 정산 연출 도중 재접속한 클라이언트는 연출을 못 보고 최종 상태만 봄(`scoring-steps`는 1회성 브로드캐스트라 재요청 경로 없음)

---

## 다음 세션 예정 작업

1. **정산 후 말풍선 일괄 소멸** — 정산 연출 시작 시(또는 스킵 시) 남아있는 베팅/이탈 말풍선을 즉시 정리
2. **싱글플레이 라운드 종료 화면 중앙 정렬 연출 이식** — 멀티의 round-end 레이아웃(카지노/보드 없이 플레이어 패널이 화면 중앙에 오는 연출)을 싱글에도 적용
3. (제안) 정산 연출 중 재접속 시 최종 상태로 즉시 스킵되도록 보정 — 현재는 연출을 아예 못 보고 최종 화면만 보임
4. (제안) 라운드 마지막 베팅의 퇴장 애니메이션 추가 검토 — 서버가 정산 전 스냅샷을 이미 보내므로 구현 여지 있음
